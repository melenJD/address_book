<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/header.ejs') %> <!-- 1 -->
  </head>
  <body>
    <%- include('../partials/nav.ejs') %> <!-- 2 -->

    <div class="contact contact-index">
      <h2>Index</h2>
      <ul>
        <% contacts.forEach(function(contact) { %> <!-- 3 -->
          <li>
            <a href="/contacts/<%= contact.id %>"><%= contact.name %></a>
          </li>
        <% }) %>
      </ul>
    </div>
  </body>
</html>